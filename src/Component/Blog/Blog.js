import React from 'react';

const Blog = () => {
    return (
        <div>
            <h1>What is context API?</h1>
            <p>What is context API explain with an example?
                The Context API can be used to share data with multiple components, without having to pass data through props manually</p>
            <h1>What is semantic tag?</h1>
            <p>A semantic element clearly describes its meaning to both the browser and the developer</p>
            <h1>What will be different of inline block and inline block element?</h1>
            <p>inline The element doesn't start on a new line and only occupy just the width it requires. You can't set the width or height. inline-block It's formatted just like the inline element, where it doesn't start on a new line. BUT, you can set width and height values.</p>
        </div>
    );
};

export default Blog;